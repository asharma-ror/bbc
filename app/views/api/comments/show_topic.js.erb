Juvia.root_url = '<%= escape_javascript(Settings.juvia_comment.url) %>';
Juvia.user_id = '<%= escape_javascript(@user_id) %>';
Juvia.last_active_at = new Date;

Juvia.username = '<%= escape_javascript(@username) %>';
Juvia.user_email = '<%= escape_javascript(@user_email) %>';
Juvia.user_image = '<%= escape_javascript(@user_image) %>';
Juvia.site_key = '<%= @site_key%>';
Juvia.topic_key = '<%= @topic_key%>';
<%
  options = { 
    :action    => 'LoadTopic',
    :container => @container,
    :html      => render(:partial => 'topic', :formats => [:html], :locals => { :topic => @topic })
  }
%>

<%= juvia_handle_response(options) %>
<%= juvia_reinstall_behavior %>

Juvia.reqLoadComment();
